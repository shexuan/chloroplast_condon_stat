<links>
#################################################
<link>

# 指定link文件路径，其数据格式为：
# chr1 start1 end1 chr2 start2 end2
file = mydata/repeat_gene.links

# 设置links曲线的半径
radius = 0.31r

# 设置贝塞尔曲线半径，该值越大越扁平，不好看
bezier_radius = 0r

# 设置link曲线的颜色
color = grey

# 设置link曲线的厚度
thickness = 2

</link>
#################################################
<link>

# 指定link文件路径，其数据格式为：
# chr1 start1 end1 chr2 start2 end2
file = /home/sxuan/geseq_gene.links.txt

# 设置links曲线的半径
radius = 0.31r

# 设置贝塞尔曲线半径，该值越大越扁平，不好看
bezier_radius = 0r

# 设置link曲线的颜色
color = yellow

# 设置link曲线的厚度
thickness = 2

</link>
#################################################
<link>

# 指定link文件路径，其数据格式为：
# chr1 start1 end1 chr2 start2 end2
file = /home/sxuan/geseq_rrn.links.txt

# 设置links曲线的半径
radius = 0.31r

# 设置贝塞尔曲线半径，该值越大越扁平，不好看
bezier_radius = 0r

# 设置link曲线的颜色
color = red

# 设置link曲线的厚度
thickness = 2

</link>
#################################################
<link>

# 指定link文件路径，其数据格式为：
# chr1 start1 end1 chr2 start2 end2
file = /home/sxuan/geseq_trn.links.data

# 设置links曲线的半径
radius = 0.31r

# 设置贝塞尔曲线半径，该值越大越扁平，不好看
bezier_radius = 0r

# 设置link曲线的颜色
color = green

# 设置link曲线的厚度
thickness = 2

</link>
###############################################
</links>
